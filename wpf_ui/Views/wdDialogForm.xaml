<Window x:Class="WpfUI.Views.wdDialogForm"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfUI.Views"
        AllowsTransparency="True"
        mc:Ignorable="d"
            xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize" WindowStyle="None"
        KeyDown="Window_KeyDown"
        Title="wdDialogForm"  WindowState="Maximized" MouseDoubleClick="Window_MouseDoubleClick">
    <Window.Background>
        <SolidColorBrush Opacity="0.7" Color="Black" />
    </Window.Background>
    <Grid x:Name="gContent" Background="Black" Height="auto" MinWidth="300"  HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0,18,0,0">
        <StackPanel VerticalAlignment="Top">
            <Grid Background="#FF0069C0" Height="30">
                <TextBlock HorizontalAlignment="Left" Margin="2,2" Name="lblWinTitle" FontSize="15">Dialog</TextBlock>
                <StackPanel HorizontalAlignment="Right" VerticalAlignment="Center" Margin="10 5">
                    <Button x:Name="btnPower" Width="20" Height="20" Background="{x:Null}" BorderBrush="{x:Null}" Padding="0" Foreground="White" Click="btnPower_Click">
                        <materialDesign:PackIcon Kind="Power" Width="20" Height="20" />
                    </Button>
                </StackPanel>
            </Grid>
        </StackPanel>
        <Frame x:Name="frameMain" NavigationUIVisibility="Hidden" Margin="10,30,10,10" Background="#FFA9A9A9"></Frame>
    </Grid>
</Window>
