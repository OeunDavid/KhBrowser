<Page x:Class="ToolKHBrowser.Views.pgChangeStore"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        mc:Ignorable="d" 
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
      
      d:DesignHeight="85" d:DesignWidth="400"
      Title="pgChangeStore">

    <StackPanel Background="Black">
        <Grid>
            <StackPanel Margin="5">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="80"/>
                        <ColumnDefinition Width="290"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="30"/>
                    </Grid.RowDefinitions>
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Left" Grid.Column="0">
                        <TextBlock Margin="2" FontSize="11" Foreground="{DynamicResource MaterialDesignBody}" VerticalAlignment="Center" HorizontalAlignment="Center"><Run Text="New Store"/></TextBlock>
                    </StackPanel>
                    <StackPanel Margin="0" Grid.Row="0" Grid.Column="1">
                        <ComboBox 
                        Name="ddlStore" 
                        SelectedValuePath="Id"
                        DisplayMemberPath="Name" 
                        Foreground="{DynamicResource MaterialDesignBody}"
                        IsSynchronizedWithCurrentItem="True"
                       />
                    </StackPanel>
                </Grid>
                <Button x:Name="btnSaveNote" Height="26" Width="130" Margin="15"  IsEnabled="True" Click="btnStartChange_Click" >
                    <StackPanel Orientation="Horizontal">
                        <materialDesign:PackIcon Kind="ContentSave" Width="15" Height="15" VerticalAlignment="Center" Margin="0"/>
                        <TextBlock Margin="0" Text="Start Change" TextAlignment="Left" VerticalAlignment="Center"/>
                    </StackPanel>
                </Button>
            </StackPanel>
        </Grid>
    </StackPanel>
</Page>
